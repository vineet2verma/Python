
import pandas as pd
import dataframe_image

# dfi.export(df_styled, 'df_styled.png')
  
# Display Data frame
# df1 = df.groupby(['Year'])['x','y'].sum().reset_index()
# df2 = df1[['x','y']].sum().rename("Total")
# print(df1.append(df2).fillna(''))
